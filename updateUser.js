const Users = require('./userSchema');

module.exports = (request, response) => {
//     Users.UsersModel.findByIdAndUpdate({_id: request.params.id}, request.body).then(() => {
//         Users.UsersModel.findOne({_id:request.params.id}).then((foundData) => {
//             response.status(200).send(foundData);
//         })
//     });
};